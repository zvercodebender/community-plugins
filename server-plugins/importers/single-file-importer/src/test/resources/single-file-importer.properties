# If the importable package directory contains many expanded DARs, it may be
# easier to scan only the root folder for non-DARs. Otherwise, files such as EARs
# and WARs inside expanded DARs will also be available for import. 
single-file-importer.scanSubdirectories=true